# Windows-compatible requirements for eDNA Biodiversity Assessment System

# Core Dependencies
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0
scikit-learn>=1.0.0

# Bioinformatics (Windows compatible)
biopython>=1.79

# Deep Learning
torch>=1.11.0
transformers>=4.15.0

# Clustering (Windows compatible alternatives)
umap-learn>=0.5.3
# Note: HDBSCAN requires compilation tools on Windows
# The system will fallback to DBSCAN if HDBSCAN is not available

# Visualization
streamlit>=1.8.0
plotly>=5.6.0
seaborn>=0.11.0
matplotlib>=3.5.0

# Data Processing
joblib>=1.1.0
tqdm>=4.62.0

# Utilities
click>=8.0.0
pyyaml>=6.0
requests>=2.27.0

# Development
pytest>=7.0.0

# Optional (install manually if needed)
# cutadapt>=3.4  # Requires compilation
# pysam>=0.17.0  # Requires compilation
# muscle>=3.8.1551  # External tool
# blast>=2.12.0  # External tool